( function ( $ ) {
    'use strict';

    $( function () {

        const $form = $( '#wpdbs-form' );
        const $cleanBtn = $( '#wpdbs-clean-btn' );
        const $spinner = $( '.wpdbs-spinner' );
        const $status = $( 'wpdbs-status' );

        const { ajaxUrl, nonce, i18n } = wpdbsSurgeon;

    } );
} )( jQuery );